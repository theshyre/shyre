/**
 * Re-exported from `@theshyre/ui`. Alias `SaveStatusValue` → `SaveStatus`
 * to preserve the existing consumer type name.
 */

export {
  useAutosaveStatus,
  type AutosaveStatus,
  type SaveStatusValue as SaveStatus,
} from "@theshyre/ui";
