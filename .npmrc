# Route @theshyre/* imports to GitHub Packages.
# Auth token lives in ~/.npmrc (local dev) or the NODE_AUTH_TOKEN env
# var (CI / Vercel). Keeping it out of this file means the repo stays
# check-in-safe and doesn't shadow the global config with an empty value.
@theshyre:registry=https://npm.pkg.github.com
