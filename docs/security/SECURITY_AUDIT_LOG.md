# Security Audit Log

> **Append-only.** Never delete entries. Update resolution when fixed.

| ID | Date | Severity | Description | Status | Resolution |
|----|------|----------|-------------|--------|------------|
| SAL-001 | 2026-04-13 | Info | Initial project setup — RLS enabled on all tables, no secrets in code, auth middleware in place | Resolved | Baseline security established in commit `e67885f` |
| SAL-002 | 2026-04-13 | Medium | `stopTimerAction`, `updateTimeEntryAction`, `deleteTimeEntryAction` filtered by entry `id` only — RLS would still block cross-user writes, but defense-in-depth missing | Resolved | Added `.eq("user_id", userId)` filter in all three actions (time-entries Phase 1 redesign) |
| SAL-003 | 2026-04-14 | Low (availability, not confidentiality) | `system_admins` SELECT policy self-joined the same table (`EXISTS (SELECT 1 FROM system_admins …)`), triggering Postgres "infinite recursion detected in policy for relation system_admins". Supabase client returned `{data: null, error}`, `requireSystemAdmin` read that as "not admin", and every `/admin/*` route redirected sysadmins to `/`. Failed closed (no privilege escalation), but the admin surfaces were unreachable. | Resolved | Policy rewritten to `USING (public.is_system_admin())` — reuses the existing SECURITY DEFINER helper from migration 006, which bypasses RLS on its internal lookup. Commit `5221fc1`, migration `20260414232000_fix_system_admins_rls_recursion.sql`. Verified sysadmins read their row; non-sysadmins see zero. |
| SAL-004 | 2026-04-14 | Low (availability, not confidentiality) | `customers_select` USING was `public.user_can_view_customer(id)`, which internally runs `SELECT team_id FROM customers WHERE id = p_customer_id`. During `INSERT … RETURNING` (used by any `supabase.from("customers").insert(...).select(...)`), Postgres evaluates the SELECT policy's USING against the NEW row; same-statement visibility means the nested SELECT can't see the new row, so the function returned false and the INSERT failed with "new row violates row-level security policy" — even for the row's legitimate owner. `loadSampleDataAction` hit this; the latent bug would also hit any future server action that wanted IDs back from a customer insert. | Resolved | Added a fast-path branch to `customers_select` that uses the NEW row's own `team_id` directly (no customers re-query): `USING (user_has_org_access(team_id) OR user_can_view_customer(id))`. Semantics unchanged — direct-org-member access was already the first branch inside `user_can_view_customer`. Commit pending, migration `20260414234000_fix_customers_select_insert_returning.sql`. Verified: Marcus (owner) can now INSERT RETURNING; strangers still see zero customers. |
| SAL-005 | 2026-04-14 | Info (refactor, not a fix) | Renamed DB tables `organizations` / `organization_members` / `organization_settings` / `organization_invites` / `organization_shares` → `teams` / `team_members` / `team_settings` / `team_invites` / `team_shares`. Renamed column `organization_id` → `team_id` on every scoped table (customers, projects, time_entries, invoices, expenses, security_groups, category_sets, time_templates, team_members, team_settings, team_invites) and `parent_org_id` / `child_org_id` → `parent_team_id` / `child_team_id` on team_shares; `error_logs.org_id` → `team_id` for uniformity. Renamed helpers `user_has_org_access` → `user_has_team_access`, `user_org_role` → `user_team_role`, `create_organization` → `create_team`, `user_can_see_cross_org_entries` → `user_can_see_cross_team_entries`, `propose_organization_share` → `propose_team_share`, `accept_organization_share` → `accept_team_share`, `change_customer_primary_org` → `change_customer_primary_team`. Policy names with "org" vocabulary (e.g. `"Org members can read their category sets"`) dropped and recreated under team-named equivalents (`category_sets_team_read`). All done via `ALTER TABLE RENAME` / `ALTER TABLE RENAME COLUMN` / `ALTER FUNCTION RENAME TO` so FK constraints, indexes, RLS policy expressions, and function-OID policy bindings carry over untouched; policy bodies auto-rewrite their column/table references on rename. RLS semantics unchanged. | Resolved | Single migration `20260415120000_rename_organizations_to_teams.sql`. Unit + component suite green (289/289) and the integration-RLS suite run against the live DB post-migration (59/59, 77s) — confirms no policy drifted, all helper functions callable under RLS, and the TS call sites with renamed named args (`team_name`, `p_parent_team_id`, `p_new_team_id`, `p_team_id`) match the new DB signatures. Postgres rejected parameter renames via `CREATE OR REPLACE` on first push attempt; resolved by DROP+CREATE for functions with no policy dependency (`create_team`, `propose_team_share`, `change_customer_primary_team`, `add_customer_share`) while keeping original parameter names on RLS-bound helpers (`user_has_team_access.org_id`, `user_team_role.org_id`, `user_can_see_cross_team_entries.p_client_id`). |
| SAL-009 | 2026-04-23 | Medium (authorization) | Harvest import API route `/api/import/harvest` called `validateTeamAccess(organizationId)` to confirm team membership but never checked the returned role — so any team **member** could trigger a bulk import that creates customers / projects / time entries in the target team. Not a privilege escalation (the caller was already a member), but members should not be able to bulk-seed operational data. Also surfaced: the importer silently attributed every imported time entry to the caller regardless of the original Harvest user, because `HarvestTimeEntry` didn't include the `user` field and the code used `user.id` unconditionally. For a multi-user Harvest account this means the caller appeared as author of co-workers' historical entries — a silent attribution bug the new EntryAuthor surface would render as fact. | Resolved | Rewrote the route with an explicit owner/admin gate (returns 403 for plain members with a friendly error). Extended `HarvestTimeEntry` with `user: { id, name }`, added a user-mapping step to the preview UI (email → display-name → "importer" fallback proposal, overridable per Harvest user with Shyre user / "importer" / "skip" choices), and routed each entry's `user_id` through `resolveEntryUserId(harvestUserId, mapping, importerUserId)`. Also fixed the unrelated-but-silent time-zone bug in the same PR — Harvest wall-clock times now parse in the account's IANA zone (`time_zone` field off `/company`) and convert to UTC correctly across DST. Added `imported_from` / `imported_at` / `import_run_id` / `import_source_id` audit columns to `customers`, `projects`, `time_entries`, `category_sets`, `categories` for idempotent re-runs and clean per-run undo. 42 unit tests cover the pure parsing / mapping / row-building helpers in `src/lib/harvest-import-logic.ts`. Migration `20260423120000_import_audit_trail.sql`. |
| SAL-008 | 2026-04-20 | Info (structural) | Added `business_people` — employment records distinct from `user_business_affiliations`. `user_id` nullable FK to `auth.users` (ON DELETE SET NULL — deleting the login does not delete the employment record; the books need that history). Partial unique index `(business_id, user_id) WHERE user_id IS NOT NULL` enforces one-record-per-linked-user-per-business while allowing multiple unlinked rows (separate contractors named the same thing are real). RLS scoped via `user_business_role(business_id) IN ('owner','admin')` for writes, `user_has_business_access(business_id)` for reads — linking a person to a user does **not** grant that user access to the business; team_members is the only auth primitive. Sensitive compliance fields (TIN, SSN, W-9-on-file, withholding) deliberately deferred to a future PR — they need encryption-at-rest decisions. | Resolved | Migration `20260420190000_business_people.sql`. `db-parity.test.ts` extended with 3 new PAIRS (`employmentTypes`, `compensationTypes`, `compensationSchedules`) — 18/18 pass. Sample-data seeds 5 people exercising owner (linked to caller), w2_employee (linked to sample users), 1099_contractor (unlinked), partner (unlinked) paths. |
| SAL-007 | 2026-04-20 | Info (structural) | Split `business` from `team`: introduced `businesses` as a first-class table, added `teams.business_id NOT NULL`, moved seven identity columns (`legal_name`, `entity_type`, `tax_id`, `state_registration_id`, `registered_state`, `date_incorporated`, `fiscal_year_start`) from `team_settings` onto `businesses`, added `user_business_affiliations` (home-business attribution), added `business_state_registrations` / `business_tax_registrations` / `business_registered_agents` with RLS scoped via new helpers `user_has_business_access(business_id)` and `user_business_role(business_id)` — both derive through `team_members` joined on `teams.business_id`. Authorization never gates on affiliation; affiliation is informational (employer of record) only. `invoices.business_id` denormalized NOT NULL, auto-populated via BEFORE INSERT trigger from `teams.business_id` so invoices remain stable if a team is ever re-parented. Signup trigger (`handle_new_user`) rewritten to create a shell business → personal team (with FK) → primary affiliation. `create_team` RPC now takes optional `p_business_id`; passing NULL creates a fresh shell business, passing a UUID requires the caller be owner/admin of an existing team in that business (enforced via `user_business_role`). `guard_teams_business_id` trigger blocks `UPDATE teams SET business_id` once the team has any `invoices` or `expenses` rows. | Resolved | Migrations `20260420170000_introduce_businesses_and_affiliations.sql`, `20260420170001_move_identity_to_businesses.sql`, `20260420170002_business_registrations.sql`. `db-parity.test.ts` extended with 7 new PAIRS (`registrationTypes`, `registrationStatuses`, `reportFrequencies`, `dueRules`, `taxTypes`, `taxRegistrationStatuses`, `filingFrequencies`) plus `affiliationRoles` — 15/15 pass. Sample-data generator seeds a Delaware LLC formation + CA/TX foreign qualifications + CSC registered agent. RLS integration tests for the new helpers are a follow-up. |
| SAL-006 | 2026-04-16 | Medium (confidentiality) | `time_entries_select` used `user_has_team_access(team_id)` as its middle clause, which meant any team member could `SELECT` any other team member's time entries — including duration, billable flag, description, and (via joined `projects` / `customers`) any rate / client detail. Write-side policies (INSERT / UPDATE / DELETE) were already tight (own-entry or owner/admin) since migration 009, but the read-side was inherited from the initial multi-tenant policy and never tightened. The user's design intent is "members log their own time and nothing else" — this policy violated that default. During an audit on 2026-04-16 I initially flagged three RLS gaps (also `user_settings` and `projects` / `customers` writes) but verification against `pg_policies` on the live DB showed those had already been resolved in migrations 004 and 009 respectively. This SAL is the one remaining real gap. | Resolved | First-attempt migration `20260416220000_sal006_tighten_time_entries_select.sql` replaced the middle clause with `user_team_role(team_id) IN ('owner', 'admin')` and preserved the existing third clause `user_can_see_cross_team_entries(p.customer_id)`. Integration tests caught a subtle bug: `user_can_see_cross_team_entries`'s FIRST branch returns TRUE for any member of the customer's primary team, silently re-opening the same-team read gate that the tightened middle clause closed — so a plain member could still read another member's entry via clause 3. Correction migration `20260416231353_sal006_correction_inline_cross_team_branches.sql` inlines the three cross-team branches explicitly (owner/admin of the customer's primary team; member of a team with `customer_shares.can_see_others_entries = true`; customer-admin permission) so same-team members no longer reach the cross-team gate by accident. Integration tests in `src/__integration__/rls/time-entries-rls.test.ts` under `describe("SAL-006: same-team member visibility is tight by default")` exercise three cases: (a) same-team member cannot read the owner's entry, (b) member can still read their own entry, (c) owner can read a member's entry. Phase 3 of `docs/reference/rate-and-access-plan.md` will add a `time_entries_visibility` config (own_only / read_all / read_write_all) on team_settings + projects that an owner can opt into for teams that want looser defaults. Lesson: a helper that looks relevant to your rule may encode other rules you don't want — prefer inlining small branches over delegating to a generic-sounding helper when the policy's tightness hinges on each branch being explicitly narrow. |
| SAL-011 | 2026-04-27 | High (confidentiality + integrity) | `invoices_select` / `invoices_insert` / `invoices_update` (added in 009_resource_sharing.sql) gated on `user_has_team_access(team_id)` — any team member could SELECT every team invoice (including totals, notes, line-item descriptions which may quote time-entry descriptions tightened in SAL-006), INSERT new invoices, and silently UPDATE any field including `status`. The status-update path was the worst: `updateInvoiceStatusAction` accepted any status with no role check and no transition guard, so a junior member could POST `id + status=void` and silently void a paid invoice. Same shape as SAL-006/SAL-010 — write-side enforced at the action layer for *create* but not *status-update*; SELECT was loose across the board. Surfaced during a parallel agency-owner + bookkeeper persona review of `/invoices`. The bookkeeper specifically called out that *no audit trail* meant a paid invoice's totals could be silently mutated with no way to prove what the customer was billed at the time. | Resolved | Migration `20260427231545_invoices_audit_and_rls_tightening.sql` does three things: (1) tightens `invoices_select` / `invoices_insert` / `invoices_update` to `user_team_role(team_id) IN ('owner','admin')`, preserving the customer-admin escape hatch on SELECT so a contractor with explicit permission on a customer can still see invoices for that customer; (2) adds the same gate on `invoice_line_items` (previously implicit via parent); (3) adds `created_by_user_id` / `updated_by_user_id` columns + auto-stamp triggers on both tables, plus append-only `invoices_history` and `invoice_line_items_history` tables (JSONB snapshot of pre-change state, owner/admin SELECT only, no client INSERT/UPDATE/DELETE — only the SECURITY DEFINER trigger can write). New `src/lib/invoice-status.ts` module defines the legal transition graph (draft→sent\|void; sent→paid\|void\|overdue; overdue→paid\|void; paid + void terminal); `updateInvoiceStatusAction` now (a) fetches current status before role check, (b) calls `validateTeamAccess` for the role gate, (c) calls `isValidInvoiceStatusTransition` to reject illegal transitions like paid→draft. Native `confirm()` for void replaced with an inline typed-confirm pattern: the user types the invoice number to arm a red Void button, hides the trigger while confirming, Escape cancels. 13 unit tests on `invoice-status.ts` cover every legal + illegal transition. |
| SAL-010 | 2026-04-27 | Medium (confidentiality) | `business_people.bp_select` policy used `user_has_business_access(business_id)`, which returns TRUE for any team member of any team in the business. That meant a 6-person shop with a junior contributor on a shared customer team could `SELECT` every coworker's `compensation_amount_cents`, `compensation_schedule`, `address_line1`, employment dates, and TIN-adjacent payroll metadata. Write policies (`bp_insert`, `bp_update`, `bp_delete`) were already correctly gated to owner/admin via `user_business_role(business_id)`; SELECT was the outlier. Surfaced during an agency-owner persona review of the Business surface — Solo Consultant lens missed it because solo data is self-data. The agency-owner review also led to the role-gate on the new Business sidebar entry: even with the policy fix, exposing the surface to non-managers in nav advertises HR data they can't see. | Resolved | Migration `20260427215057_business_people_role_gated_select.sql`. New policy: `user_business_role(business_id) IN ('owner','admin') OR user_id = auth.uid()` — owner/admin of the business sees everything; other users see only their own row (so a linked employee can still view their own profile). Sidebar entry for `/business` is now role-gated in `src/components/Sidebar.tsx` via a `canManageBusiness` prop computed in `(dashboard)/layout.tsx` from `team_members.role IN ('owner','admin')`, hiding the entry for users who would hit a near-empty page anyway. Tightening migration — no code today depended on the broader read access (only consumer is `/business/[id]/people` which is an owner/admin surface), so safe to ship code + migration together. |
| SAL-012 | 2026-04-28 | Medium (confidentiality) | Same shape as SAL-010 / SAL-011, but for business identity & registrations. (1) `bsr_select` / `btr_select` / `bra_select` on `business_state_registrations` / `business_tax_registrations` / `business_registered_agents` rode `user_has_business_access(business_id)` — any team member could SELECT state registration IDs, state tax IDs, and registered-agent contact info. (2) `businesses.tax_id` / `date_incorporated` / `fiscal_year_start` were on the wide `businesses` table whose SELECT policy correctly returns the row to all team members so they can see the business name on detail-page headers — which meant a member running `select("tax_id")` against PostgREST got the company's EIN. The UI form already gated rendering by role, but the DB-layer leak persisted. Surfaced by the security-reviewer + agency-owner personas in the 2026-04-28 whole-project audit. | Resolved | Expand: Migration `20260428024320_sal012_business_identity_rls.sql`: (a) tightens `bsr_select` / `btr_select` / `bra_select` to `user_business_role(business_id) IN ('owner','admin')`; (b) creates 1:1 child table `business_identity_private (business_id, tax_id, date_incorporated, fiscal_year_start)` with all four CRUD policies role-gated, an `AFTER INSERT ON businesses` trigger that auto-creates the empty private row, an actor-stamping `BEFORE INSERT/UPDATE` trigger, and `business_identity_private_history` mirroring the existing `*_history` pattern; (c) backfills the new table from the existing columns then NULLs `businesses.tax_id` / `date_incorporated` / `fiscal_year_start` so a member doing `select("tax_id")` gets only NULL. Identity page now reads from `business_identity_private` only when `canEdit` is true, and `IdentityForm` hides the three sensitive inputs entirely for non-admins via a new `canEditPrivate` prop. `updateBusinessIdentityAction` and the sample-data writer split their UPDATE into the two tables. `mergeIdentityHistoryRows` accepts an optional `privateRows` arg so the timeline still shows EIN / incorporation-date changes for owner|admin viewers. **Contract**: Migration `20260428032312_sal012_drop_deprecated_business_identity_columns.sql` drops the now-NULL columns from `businesses` after a code-grep confirmed no production reader still touched them. Existing `businesses_history` rows keep the dropped values inside their JSONB `previous_state` blobs and the timeline label map preserves the labels so old entries still render. |
| SAL-013 | 2026-04-28 | Medium (confidentiality) | `expenses_select` (from `20260414230000_expenses.sql`) used `user_has_team_access(team_id)`, so every team member could SELECT every other member's expenses — vendor, amount, description, project, billable flag. Write policies were already tight (INSERT requires `user_id = auth.uid()`, UPDATE/DELETE require author OR owner/admin); SELECT was the outlier. Same pattern as SAL-006 / SAL-010 / SAL-011 / SAL-012. Also: `updateExpenseAction` and `deleteExpenseAction` had no explicit role check at the action layer — they relied entirely on RLS, so a denied write surfaced as an opaque RLS error rather than a friendly "only the author or an owner/admin can edit." | Resolved | Migration `20260428024606_sal013_tighten_expenses_select.sql` rewrites `expenses_select` USING to `user_id = auth.uid() OR public.user_team_role(team_id) IN ('owner','admin')` — author OR owner/admin only. `updateExpenseAction` / `deleteExpenseAction` now fetch the row first, call `validateTeamAccess` for the role, and reject with a friendly error when the caller is neither the author nor owner/admin (defense in depth + better UX). |
| SAL-014 | 2026-04-28 | Critical (SSRF + auth bypass) | `testJiraCredsAction` and the persisted `lookupTicket` → `fetchJiraIssue` path (added 2026-04-28 with the ticket-linking feature) accepted any user-supplied Jira base URL whose only validation was `^https?://`. Two attack surfaces: (1) the Test-connection action let any logged-in user POST `baseUrl=http://169.254.169.254/...` and read the response status / message — a network probe of the Vercel runtime's internal network including AWS IMDS. (2) After saving the malicious base URL via `updateUserSettingsAction` (same weak regex), every subsequent time-entry save with a Jira-shaped key in the description fired `fetch()` against the attacker-chosen URL with the user's email + Jira API token in a Basic-auth header, leaking creds on every save. Compounding: `testJiraCredsAction` and `testGithubTokenAction` were exported `"use server"` actions but never called `auth.getUser()` and weren't wrapped in `runSafeAction`, so they were reachable by any forged action POST and never logged failures via `logError()`. | Resolved | New `src/lib/url-safety.ts` exports `assertSafeOutboundUrl(url, options?)` — parses the URL, requires `https://`, blocks literal IPs in private / loopback / link-local / CGNAT / multicast / reserved ranges (IPv4 + IPv6, including `::ffff:` mapped), DNS-resolves the host and rejects if any returned record falls in the same ranges (defends against DNS rebinding). `fetchJiraIssue` and `validateJiraCreds` now call it before every fetch and pass `redirect: "manual"` so a 30x to a private host can't bypass. The save-side validation in `updateUserSettingsAction` was tightened from `^https?://` to `^https://` so a stored base URL can't silently downgrade. Both Test-connection actions now require `auth.getUser()` (returning a generic "Unauthorized" on miss) and call `logError()` on non-401 / non-network failures so probe attempts are visible in `/admin/errors`. The `validateBusinessAccess` JSDoc gained a warning that the returned role is aggregate, not per-team — and the period-locks page now filters teamOptions to `role IN ('owner','admin')` instead of any membership, closing a defense-in-depth gap where a member of a co-team would have seen that team in the lock dropdown. |
| SAL-015 | 2026-05-03 | Medium (confidentiality) | The pre-existing `user_settings.github_token TEXT` column was NOT encrypted at rest — code comments and CLAUDE.md commentary implied "encrypted secret" but the column was plain text protected only by RLS. Surfaced during the messaging-platform security review when I proposed putting the new Resend API key on `team_settings` "alongside the encrypted github_token." The reviewer's audit caught the doc-vs-reality drift. Threat model: a leaked `pg_dump`, a compromised Supabase admin, or any code path that mistakenly does `select("*")` exposes plaintext tokens. RLS is the existing mitigation but it's not defense-in-depth. | Resolved | (1) New module `src/lib/messaging/encryption.ts` provides AES-256-GCM `encryptSecret()` / `decryptSecret()` keyed by a Vercel-side `EMAIL_KEY_ENCRYPTION_KEY` env var (32-byte hex). 12-byte IV + 16-byte auth tag prepended to ciphertext; tampering rejected by the auth-tag check. 6 unit tests cover the round-trip, tamper rejection, BYTEA-hex parsing. (2) Migration `20260503000000_messaging_platform.sql` creates `team_email_config.api_key_encrypted BYTEA` (encrypted from day one) and adds a parallel `user_settings.github_token_encrypted BYTEA` column for the existing GitHub token. Phase 2 will migrate the GitHub read path to the encrypted column, then drop the plaintext column in a separate migration per CLAUDE.md's two-PR rule. (3) `.env.example` and `docs/guides/admin/env-configuration.md` document `EMAIL_KEY_ENCRYPTION_KEY` generation (`openssl rand -hex 32`), durability requirements (lose the key = unrecoverable secrets), and the dev-vs-prod key separation. |
| SAL-016 | 2026-05-03 | High (forgery) | The Send-Invoice action without a from-domain check would let a logged-in team member specify any `from` address — including a competing team's verified domain or an arbitrary spoof like `ceo@victim.com`. Trusting Resend's own domain check alone is one layer; defense-in-depth requires Shyre to reject mismatches before the request leaves our perimeter. | Resolved | New `verified_email_domains` table mirrors Resend's domain-verification state with `(team_id, domain, status)` rows. `assertFromDomainAllowed(supabase, teamId, fromEmail)` in `src/lib/messaging/outbox.ts` extracts the domain from the from-address, looks up the row scoped to the calling team, and refuses to enqueue if `status !== 'verified'`. Called from both `sendInvoice()` (real send) and `sendTestEmailAction` (test send), with consistent error messaging that links the user to `/teams/[id]/email` to add DNS records. RLS on `verified_email_domains` is owner/admin only — the table is also a secret-adjacent surface. |
| SAL-017 | 2026-05-03 | Medium (integrity) | The Resend webhook endpoint at `/api/messaging/webhook/resend` mutates `message_outbox.status` and `customers.bounced_at` based on whatever payload arrives. Without signature verification, an attacker could POST forged "delivered" / "bounced" / "complained" events to the public endpoint — flipping invoice statuses or marking customers as bounced (which then suppresses future legitimate sends). | Resolved | Standard-Webhooks (svix-style) signature verification: `computeSignature(secret, id, timestamp, rawBody)` → HMAC-SHA256 base64; `timingSafeEqual` against the `svix-signature` header value. Multi-signature header support for key rotation. 5-minute replay window on `svix-timestamp`. Returns 401 on missing or mismatched signatures, 401 on stale timestamps. Webhook secret stored in `RESEND_WEBHOOK_SECRET` env var; `whsec_`-prefixed secrets are base64-decoded per Resend's docs. |

| SAL-018 | 2026-05-03 | Medium (defense-in-depth) | Phase 1 of the messaging platform encrypted every team's API key directly with the single instance master key (KEK from EMAIL_KEY_ENCRYPTION_KEY). Multi-tenant aspiration calls for per-tenant key isolation: a leaked DEK should expose only one team, KEK rotation should not require re-encrypting every secret in the database, and audit posture should show that one team's secret is sealed against another team's compromise. Phase 1 met none of those properties — the user pointed this out as part of the deploy-automation conversation. | Resolved | Migration `20260503100000_envelope_encryption.sql` adds `team_email_config.dek_encrypted BYTEA` (per-team data-encryption key, wrapped with KEK). Encryption module gains `generateDek`, `wrapDek`, `unwrapDek`, `encryptWithDek`, `decryptWithDek` plus the high-level `getOrCreateTeamDek`, `encryptForTeam`, `decryptForTeam` helpers. Action layer (`/teams/[id]/email/actions.ts` + `test-send-action.ts` + `messaging/send-invoice.ts`) now uses `encryptForTeam` / `decryptForTeam` exclusively for team-scoped secrets. Backward compatibility: `decryptForTeam` tries the team's DEK first; on failure (no DEK row yet, or auth-tag mismatch on the DEK path) it falls back to legacy direct-KEK decryption — Phase-1 ciphers stay readable until the next save naturally rolls them forward to DEK-encryption. No big-bang backfill required. Tests: `encryption.test.ts` extended with 9 new cases covering DEK generation, wrap/unwrap, per-team isolation (DEK A cannot decrypt cipher made with DEK B), tamper rejection on wrapped DEKs. The legacy `encryptSecret` / `decryptSecret` exports remain for non-team contexts (e.g. `instance_deploy_config.api_token` in SAL-019, which is system-wide rather than team-scoped). |
| SAL-019 | 2026-05-03 | Medium (privileged secret) | Adding deployment-environment automation (Vercel API token in `instance_deploy_config`) created a new high-privilege secret in the schema. The token grants Shyre write access to the user's Vercel project env vars and the ability to trigger redeploys. Storing it as plaintext TEXT mirrors the SAL-015 lineage on `user_settings.github_token` (RLS-only protection, system-admin scope). Encrypting under the master key would be the upgrade — but the chicken-and-egg cost of doing so during the bootstrap UX (where the user is provisioning the master key for the first time *via* this very table's stored token) makes Phase 1 of automation deliberately use the same RLS-only pattern as github_token. The token is RLS-protected by `is_system_admin()` and never returned in list queries; reads happen only inside server actions that have already passed the system-admin gate. | Resolved | Migration `20260503110000_instance_deploy_config.sql` adds the table with system-admin-only RLS policies on every CRUD verb (SELECT/INSERT/UPDATE/DELETE all gated on `is_system_admin()`). Single-row constraint via `CHECK (id = 1)` — there's one Shyre instance per deployment. Server actions in `/system/deploy/actions.ts` validate `isSystemAdmin()` even though RLS would also block (defense in depth, friendlier error). The `provisionEncryptionKeyAction` requires an explicit `confirm=regenerate` form value when `EMAIL_KEY_ENCRYPTION_KEY` is already set in the running deployment — protects against an admin accidentally rotating the master key and orphaning every stored team API key. `setEnvVarAction` is allow-listed: it only writes `RESEND_WEBHOOK_SECRET`, never an arbitrary key the action POST might smuggle. Webhook-secret value is never written back to Shyre's DB; it lives only in Vercel's encrypted env once provisioned. Phase 2 will encrypt `instance_deploy_config.api_token` under the master key and update SAL-019 with the resolution. |
| SAL-020 | 2026-05-04 | Medium (defense-in-depth) | Send-path security review flagged that `from_email` (read from `team_email_config.from_email` or the per-send override) reached the Resend envelope without going through `sanitizeHeaderValue`. Subject, from-name, and reply-to were sanitized; from-address was not. Real-world exploit was blocked by Resend's JSON transport (CR/LF in a JSON string field becomes literal characters, not a header break, so a stored `from_email = "x@y.com\r\nBcc: a@b.com"` doesn't actually inject a Bcc), but the JSON transport is the wrong place to rely on for header safety — a future provider that takes raw SMTP would have nothing protecting it. | Resolved | Commit `33e766d` passes `from_email` through `sanitizeHeaderValue` symmetrically with the other header-bound fields in `src/lib/messaging/send-invoice.ts`. Recipients (To/Cc/Bcc) continue to flow through `validateRecipient`, whose EMAIL_RE rejects any `\r\n`-bearing input by construction — same vector closed for them, just by a different gate. |
| SAL-021 | 2026-05-04 | Medium (abuse-resistance) | The Phase-1 daily-cap implementation in `consumeDailyQuota` had two issues. First, it counted each `sendInvoice` call as 1 regardless of recipient count — a 5-recipient invoice with `sendCopyToMe` burned 1 of 200 cap slots while emitting 6 envelopes, defeating the abuse-defense purpose of the cap. Second, the read-then-write was racy: two concurrent sends both reading `sent=199, cap=200` would both update to 200 and both proceed, silently bypassing the cap under load. | Resolved | Migration `20260504140000_consume_daily_quota_rpc.sql` adds a `consume_daily_quota(p_team_id, p_amount) → (allowed, remaining, cap, reason)` plpgsql function. `FOR UPDATE` on the `team_email_config` row serializes concurrent calls; conditional UPDATE rejects in one shot when `current_count + p_amount > cap`. The TS `consumeDailyQuota` now takes `recipientCount` and forwards it to the RPC; `sendInvoice` passes `toEmails.length + ccEmails.length + bccEmails.length` as the consume amount. Commit `33e766d`. |
| SAL-022 | 2026-05-04 | Low (smell) | `assertFromDomainAllowed` in `src/lib/messaging/outbox.ts` looked up the verified domain via `.ilike("domain", domain)` where `domain` was a slice of the user-controlled `from_email`. PostgREST's `.ilike` treats `%` and `_` as wildcards. A `from_email = "x@%.com"` would have matched any verified `.com` domain on the team — but only the team's own verified domains (the query is team-scoped), so the worst case was "matches a domain you already own." String-injection smell rather than escalation. | Resolved | Two-character change: `.ilike` → `.eq`. Verification flow already stores domain values lowercased; the slice is lowercased in the same function. Commit `33e766d`. |
| SAL-023 | 2026-05-04 | Low (integrity) | `recordEvent` in `src/lib/messaging/outbox.ts` inserted a fresh `message_outbox_events` row on every webhook delivery. Resend (via svix) retries on 5xx, so a transient handler failure produced two events for one logical delivery — no security exposure, but the retry's `flagCustomerBounce` re-ran and slid `customers.bounced_at` forward, and the activity log later picked up the duplicate event row. | Resolved | Migration `20260504150000_message_outbox_events_svix_id.sql` adds a `svix_id` column with a partial unique index. `recordEvent(...)` now takes the header `svix-id` as a 4th argument; the insert lands on `23505` for a duplicate, the function returns `false`, and the webhook route short-circuits the side effects (`route.ts:115-128`). Test coverage in `route.test.ts` includes the dedup path explicitly. Commit `33e766d`. |
